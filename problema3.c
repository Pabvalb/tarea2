#include <stdio.h>
int main()
{
    float dolares, peso;
    float dolares cambio = 11.96;
    printf("Ingrese la cantidad de dolares: ");
    scanf("%f, &dolares);
    pesos= dolares * dolares cambio;
    printf("la cantidad en pesos es: %.2f\n", pesos);
    return 0;
}
